# feature_tape_v2 — Unavailable Fields Registry

**Version:** v2 (post-audit)
**Audit date:** 2026-03-12
**Status:** These fields are excluded from the v2 sweep plan. They must NOT appear as 0%-coverage ambiguous columns.

---

## Excluded Fields

| Field | Reason | Source Checked | Reinstatement Condition |
|-------|--------|----------------|------------------------|
| `buy_count_1m` | No 1m transaction count aggregates exist in `microstructure_log` or `universe_snapshot`. Only `rv_1m` (realized volatility) exists at 1m resolution. | `microstructure_log` (PRAGMA table_info), `universe_snapshot` (PRAGMA table_info) | Add 1m count aggregation to the scanner/micro collector |
| `sell_count_1m` | Same as above | Same | Same |
| `buy_usd_1m` | No 1m USD volume aggregates in any source table | Same | Same |
| `sell_usd_1m` | No 1m USD volume aggregates in any source table | Same | Same |
| `signed_flow_1m` | No 1m signed flow in any source table | Same | Same |
| `buy_sell_ratio_1m` | No 1m buy/sell ratio in any source table | Same | Same |
| `avg_trade_usd_1m` | No 1m average trade size in any source table | Same | Same |
| `txn_accel_m1_vs_h1` | Requires 1m transaction count; not available | Same | Same |
| `vol_accel_m1_vs_h1` | Requires 1m volume; not available | Same | Same |
| `median_trade_usd_5m` | Not stored in `microstructure_log`. Only `avg_trade_usd_m5` is available. | `microstructure_log` (PRAGMA table_info) | Add median computation to micro collector |
| `max_trade_usd_5m` | Not stored in `microstructure_log` | Same | Same |
| `signed_flow_5m` | Not stored in `microstructure_log`. The table has `buys_m5`, `sells_m5`, and `buy_sell_ratio_m5` but no pre-computed signed flow (buys_usd - sells_usd). | `microstructure_log` (PRAGMA table_info) | Add signed_flow_m5 = buy_usd_m5 - sell_usd_m5 to micro collector; note that buy_usd_m5/sell_usd_m5 also do not exist — only counts and ratios are stored |

---

## Confirmed Available Fields (v2 Scope)

The following fields ARE in scope for the v2 sweep:

**Family A — Order-Flow (micro-native, NULL if no micro row):**
- `buys_m5`, `sells_m5`, `buys_h1`, `sells_h1`
- `buy_sell_ratio_m5`, `buy_sell_ratio_h1`
- `buy_count_ratio_m5`, `buy_count_ratio_h1`
- `avg_trade_usd_m5`, `avg_trade_usd_h1`
- `vol_accel_m5_vs_h1`, `txn_accel_m5_vs_h1`
- `r_m5_micro`, `rv_5m`, `rv_1m`, `range_5m`

**Family A — Order-Flow (snapshot-native, always available):**
- `buys_m5_snap`, `sells_m5_snap`
- `buy_count_ratio_m5_snap`, `avg_trade_usd_m5_snap`

**Family B — Route/Quote (snapshot-native, ~100% coverage):**
- `jup_vs_cpamm_diff_pct`, `round_trip_pct`
- `impact_buy_pct`, `impact_sell_pct`, `impact_asymmetry_pct`

**Family C — Market-State:**
- `liq_change_pct`, `liq_cliff_flag` (micro-native)
- `breadth_positive_pct`, `breadth_negative_pct` (derived_from_micro, fire-level)
- `median_pool_r_m5`, `pool_dispersion_r_m5`, `median_pool_rv5m` (derived_from_micro, fire-level)
- `pool_liquidity_median`, `pool_vol_h1_median` (derived_from_snapshot, fire-level)
- `pool_size_total`, `pool_size_with_micro`, `coverage_ratio_micro`

**Fundamentals (snapshot-native):**
- `age_hours`, `liquidity_usd`, `vol_h1`, `vol_h24`, `price_usd`
- `r_m5_snap`, `r_h1_snap`

**Classification:**
- `lane`, `lane_source`, `venue`, `pool_type`, `pumpfun_origin`, `eligible`, `gate_reason`

---

## Note on lane

`universe_snapshot.lane` is always NULL (confirmed: 171,731 rows, 0 non-null). Lane must be derived at collection time using the canonical logic documented in `feature_tape_v2_source_map.md`. Every row must have a non-null `lane` and `lane_source = "derived_at_collection"`.

---

*Generated by audit on 2026-03-12.*
