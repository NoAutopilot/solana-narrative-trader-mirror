# Change Manifest

**change_name:** Introduce dashboard sync policy and compatibility gate  
**commit_hash:** (pending — see ops commit)  
**utc:** 2026-03-11T19:00:00Z  

---

## Components Changed

| Field | Value |
|-------|-------|
| `component_changed` | ops, report |
| `dashboard_updated` | no, because no visible fields changed |
| `affected_dashboard_fields` | none |
| `canonical_report_affected` | no |
| `sample_definition_changed` | no |
| `counters_changed` | no |
| `labels_changed` | no |

---

## Summary

This change introduces process tooling only. No observer, sidecar, feature tape, strategy, or experiment logic was modified.

**Files added:**
- `ops/dashboard_compat_check.py` — automated DB + route + column compatibility check
- `ops/dashboard_sync_policy.md` → `reports/ops/dashboard_sync_policy.md`
- `reports/ops/dashboard_compat_latest.md` — generated by compat check (all views PASS)
- `reports/ops/change_manifest_20260311T190000Z.md` — this file

**Dashboard compatibility check result:** PASS (all views)  
See `reports/ops/dashboard_compat_latest.md` for full output.

---

## Dashboard Impact Assessment

`dashboard_compat_check.py --view all` was run and returned OVERALL: PASS.

All required columns are present in all observer DBs and feature_tape_v1.

Notable WARNs (not failures — no action required now):
- All observer services are inactive (expected — observers are in archived state)
- Dashboard HTTP returns 404 on some routes (dashboard is running but those routes are not the root `/`)
- Extra columns exist in observer tables beyond the 10 required baseline columns — these are handled gracefully by the dashboard (it selects `SELECT *` and uses only what it needs)

No dashboard code changes required for this commit.
